# YAML Chatbot Agent Instructions

This project is an AI chatbot that generates a zip file containing a set of YAML files based on user requests.

## How to run the chatbot

1. Install the dependencies:
   ```bash
   pip install -r requirements.txt
   ```

2. Run the chatbot:
   ```bash
   python chatbot.py
   ```
